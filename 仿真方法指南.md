# CANN 8.5.0 算子 CPU 仿真方法指南

本文档详细介绍了在 Mac (Apple Silicon) 上通过 Docker 容器对 CANN 8.5.0 算子进行 CPU 仿真的各种方法。

## 目录

1. [环境准备](#1-环境准备)
2. [方法一：PEM DaVinci 仿真器（推荐）](#2-方法一pem-davinci-仿真器推荐)
3. [方法二：PEM 性能 Profiling](#3-方法二pem-性能-profiling)
4. [方法三：msprof op simulator](#4-方法三msprof-op-simulator)
5. [方法四：CPU Debug 模式（tikicpulib）](#5-方法四cpu-debug-模式tikicpulib)
6. [方法五：GDB 调试内核代码](#6-方法五gdb-调试内核代码)
7. [方法六：mssanitizer 正确性检查](#7-方法六mssanitizer-正确性检查)
8. [方法七：msopgen sim 流水线可视化](#8-方法七msopgen-sim-流水线可视化)
9. [方法对比速查](#9-方法对比速查)

---

## 1. 环境准备

所有操作都在 Docker 容器内进行。CANN 安装在容器内的 `/usr/local/Ascend/cann-8.5.0/`，不在 Mac 宿主机上。

```bash
# 1. 进入容器
cd /Users/xuwenqiang/Desktop/workspace/ascend_cpu
bash enter.sh

# 2. 检查环境（容器内执行）
bash check_env.sh
# 预期输出：
#   ASCEND_HOME_PATH=/usr/local/Ascend/cann-8.5.0
#   /usr/local/Ascend/cann-8.5.0/bin/atc
#   /usr/bin/gdb
```

### 关键路径速查

| 组件 | 路径 |
|------|------|
| CANN 安装 | `/usr/local/Ascend/cann-8.5.0/` |
| 仿真器库 | `/usr/local/Ascend/cann-8.5.0/tools/simulator/Ascend910B1/lib/` |
| ASC 编译器 | `/usr/local/Ascend/cann-8.5.0/bin/bisheng` |
| tikicpulib | `/usr/local/Ascend/cann-8.5.0/tools/tikicpulib/` |
| 算子示例 | `/workspace/ascend_cpu/asc-devkit-8.5.0/examples/` |

---

## 2. 方法一：PEM DaVinci 仿真器（推荐）

### 原理

```
算子二进制 (ASC 编译器编译)
      │  调用 aclInit() → aclrtSetDevice() → kernel<<<>>>
      │
      ├── LD_PRELOAD=libnpu_drv.so           ← 替换真机 NPU 驱动
      ├── LD_PRELOAD=libruntime_cmodel.so    ← 替换真机运行时
      └── LD_PRELOAD=libpem_davinci.so       ← PEM 周期精确仿真引擎
              │
              └── 在 CPU 上模拟 24 个 AI Core，执行算子指令
```

PEM (Programmable Execution Model) 是 CANN 自带的 DaVinci 架构周期精确仿真器。它模拟完整的芯片行为：24 个 AI Core、Cube/Vector/Scalar 计算单元、MTE 搬运单元、L1/L0 缓存等。

### 基本用法

```bash
# === 1. 设置环境（容器内） ===
source /etc/profile.d/ascend.sh

SIMDIR=/usr/local/Ascend/cann-8.5.0/tools/simulator/Ascend910B1/lib
ASCENDLIB=/usr/local/Ascend/cann-8.5.0/lib64
DEVLIB=/usr/local/Ascend/cann-8.5.0/devlib/aarch64

export LD_LIBRARY_PATH=${SIMDIR}:${ASCENDLIB}:${DEVLIB}:${LD_LIBRARY_PATH}

# === 2. 编译算子 ===
cd /workspace/ascend_cpu/asc-devkit-8.5.0/examples/00_introduction/01_add/basic_api_tque_add

cmake -B build-cpu -DCMAKE_ASC_RUN_MODE=cpu
cmake --build build-cpu -j

# === 3. 生成测试数据 ===
cd build-cpu
mkdir -p input output
python3 ../scripts/gen_data.py   # 如果有 gen_data.py

# === 4. 运行仿真 ===
LD_PRELOAD="libnpu_drv.so:libruntime_cmodel.so:libpem_davinci.so" ./add
```

### 运行日志解读

```
[INFO] AicWrapper attach AIC 0, num_vec_core=2, num_subcore=3   ← Core 0: 2 VectorCore + 3 SubCore
[INFO] AicWrapper attach AIC 1, num_vec_core=2, num_subcore=3   ← Core 1
...
[INFO] AicWrapper attach AIC 23, num_vec_core=2, num_subcore=3  ← 共 24 个 Core

[INFO] Chip 0 AIC / Scheduler / Soc periods:
       200.0000 / 200.0000 / 105.0000            ← 各时钟域周期（ps）

>>>>  " PEM MODEL "                                 ← PEM 仿真模型已就绪
>>>>  Total no. of 1 chip(s) Model Init Success!

[INFO] Model Start Time: 2026-06-10 11:18:16
[DRVSTUB_LOG] driver_api.c:556 sendSwapBuf ...      ← 驱动 stub 日志
[INFO] Model Stop Time: 2026-06-10 11:18:19
Model RUN TIME: 2205.29 ms                          ← 仿真真实耗时（不代表 NPU 性能）
[INFO] Total tick: 8901                             ← 总 cycle 数（周期精确）

Output:  3.5 3.5 3.5 ...                            ← 算子输出
Golden:  3.5 3.5 3.5 ...                            ← 参考结果
[Success] Case accuracy is verification passed.     ← 精度验证通过
```

> **注意**：`Model RUN TIME` 是仿真器运行的真实时间，受 CPU 性能影响。`Total tick` 才是周期精确的性能指标，用于分析算子的计算效率。

### 一键运行脚本

已封装在 [`run_operator.sh`](run_operator.sh)：

```bash
# 在容器内
bash run_operator.sh add           # 单个算子
bash run_operator.sh matmul        # 矩阵乘法
bash run_operator.sh add sub reduce matmul  # 批量
```

---

## 3. 方法二：PEM 性能 Profiling

PEM 仿真器运行时自动生成 `profile_*.toml` 文件，包含每个 block/core 的详细 cycle 数据。

### 操作步骤

```bash
# 1. 进入算子的 build-cpu 目录，清理旧的 profile 文件
cd /path/to/build-cpu
rm -f profile_*.toml

# 2. 设置环境和运行（同方法一）
export LD_LIBRARY_PATH=${SIMDIR}:${ASCENDLIB}:${DEVLIB}:${LD_LIBRARY_PATH}
LD_PRELOAD="libnpu_drv.so:libruntime_cmodel.so:libpem_davinci.so" ./add

# 3. 查看 profiling 结果
ls profile_*.toml
```

### profile 文件详解

PEM 生成 7 个 toml 文件，覆盖从 chip 到 block 的各级 profiling：

| 文件 | 内容 | 关键指标 |
|------|------|----------|
| `profile_network_log0.toml` | 芯片级汇总 | `total_cycles`, `num_streams` |
| `profile_stream_log0.toml` | Stream 级 | `start_cycle`, `end_cycle`, `duration`, `wait_cycle` |
| `profile_task_log0.toml` | Task 级 | `duration`, `wait_cycle`（调度等待） |
| `profile_subtask_log0.toml` | Subtask 级 | `block_dim`, `core_list`, `thread_dim`, `type` |
| `profile_aiv_log0.toml` | **每个 Block 详细信息** | `block_id`, `core_id`, `start_cycle`, `end_cycle`, `duration` |
| `profile_aic_log0.toml` | AI Core 信息 | - |
| `profile_dmu_log0.toml` | DMU（数据搬运单元） | - |

### 实际案例：add 算子 profiling 分析

以 add 算子（8 个 block，运行在 8 个 core 上）为例：

**profile_network_log0.toml**（芯片级汇总）:
```toml
chip_version = "cloud_v220"     # 仿真芯片版本 (Ascend910B)
[network]
total_cycles = 5469              # 总耗时 5469 cycles
```

**profile_subtask_log0.toml**（Subtask）:
```toml
[network.stream2.task0.subtask0.thread0]
    type = "AIV"                 # 算子类型: AIV（AI Vector Core）
    block_dim = 8                # 8 个 block 并行
    core_list = [0,1,2,3,4,5,6,7]  # 分布在 core 0~7
    duration = 5469              # 总耗时
    first_blk_start_cycle = 2035 # 第一个 block 启动 cycle
```

**profile_task_log0.toml**（Task 调度分析）:
```toml
[network.stream2.task0]
    duration = 5469              # 执行耗时
    wait_cycle = 2322            # 调度等待：2322 cycles
```
> **分析**：调度等待（wait_cycle=2322）占总耗时的 42%，说明 kernel launch 开销较大。

**profile_aiv_log0.toml**（每个 Block 明细）:
```toml
[network.stream2.task0.subtask0.thread0.block0]
    block_id = 0, core_id = 0
    start_cycle = 2035, end_cycle = 7494
    duration = 5459               # Block 0 耗时 5459 cycles

[network.stream2.task0.subtask0.thread0.block1]
    block_id = 1, core_id = 1
    start_cycle = 2036, end_cycle = 7494
    duration = 5458               # Block 1 耗时 5458 cycles（均衡 ✅）
...
```
> **分析**：8 个 block 耗时在 5458~5459 cycles 之间，分布均匀，说明负载均衡良好。

### trace.json 流水线可视化

PEM 还生成 `OPPROF_xxx/simulator/trace.json`，可在 Chrome 中可视化：

```bash
# 找到 trace.json
ls OPPROF_*/simulator/trace.json

# 在 Chrome 地址栏输入 chrome://tracing
# 点击 Load，选择 trace.json 文件
# 即可看到各 Core 的流水线时间轴
```

---

## 4. 方法三：msprof op simulator

`msprof op simulator` 是 CANN 8.5.0 的仿真 profiling 命令，能提供比 PEM toml 更丰富的硬件指标。

### 命令格式

```bash
msprof op simulator <可执行文件> [选项]
```

**关键选项**：

| 选项 | 说明 | 可选值 |
|------|------|--------|
| `--aic-metrics` | 采集的硬件指标 | `PipeUtilization`, `ResourceConflictRatio`, `PMSampling` |
| `--output` | 输出目录 | 任意路径 |
| `--timeout` | 超时（分钟） | 1~2880 |
| `--kernel-name` | 指定 kernel 名称 | kernel 的 mangled name |
| `--launch-count` | 采集的 kernel 次数 | 1~5000 |

### 用法示例

```bash
source /etc/profile.d/ascend.sh

# 进入算子 build 目录
cd /path/to/build-cpu

# 设置库路径
export LD_LIBRARY_PATH=/usr/local/Ascend/cann-8.5.0/tools/simulator/Ascend910B1/lib:...

# 运行 profiling
msprof op simulator \
    ./add \
    --aic-metrics=PipeUtilization \
    --output=./prof_output \
    --timeout=5
```

### 8.5.0 的限制

| 功能 | 状态 | 说明 |
|------|------|------|
| 基础仿真运行 | ✅ | 能正常启动和运行 |
| PipeUtilization | ⚠️ | 部分支持 |
| PC start addr | ❌ | stub func 不支持 `rtKernelGetAddrAndPrefCntOrigin` |
| 代码调用栈 | ❌ | stub 限制 |
| 指令级分析 | ❌ | stub 限制 |

> **建议**：日常用 PEM toml 分析 cycle 分布，需要硬件指标时升级到 CANN 9.0+，新版 `msprof op --simulator` 支持 `ArithmeticUtilization`, `Memory`, `MemoryL0`, `MemoryUB`, `L2Cache`, `Roofline` 等完整指标。

---

## 5. 方法四：CPU Debug 模式（tikicpulib）

CPU Debug 模式使用 `tikicpulib`，将算子代码编译为原生 CPU 可执行程序，支持 **printf 调试** 和 **GDB 断点**。

### 适用场景

- 核函数内添加 printf 打印中间值
- GDB 逐行调试 kernel 代码
- 不需要周期精确仿真，只需要逻辑验证

### CMakeLists.txt 写法

```cmake
cmake_minimum_required(VERSION 3.16)
project(cpudebug)

set(CMAKE_PREFIX_PATH $ENV{ASCEND_INSTALL_PATH}/tools/tikicpulib/lib/cmake)
find_package(tikicpulib REQUIRED)

add_executable(add ${CMAKE_CURRENT_SOURCE_DIR}/add.cpp)

target_compile_options(add PRIVATE
    -g -O2 -std=c++17 -D_GLIBCXX_USE_CXX11_ABI=0 -Wall -Werror
)

target_link_libraries(add PRIVATE
    ascendcl
    tikicpulib::${SOC_VERSION}
)
```

注意与 ASC 编译模式的区别：

| | CPU Debug (tikicpulib) | ASC 编译 |
|------|------|------|
| CMake | `find_package(tikicpulib)` | `find_package(ASC)` |
| 源文件 | `.cpp`（标准 C++） | `.asc`（AscendC 语法） |
| 编译选项 | `-D_GLIBCXX_USE_CXX11_ABI=0` | `--npu-arch=dav-2201` |
| 链接库 | `ascendcl + tikicpulib::Ascend910B1` | `tiling_api + register + ...` |

### 编译运行

```bash
# 已在容器内
cd /workspace/ascend_cpu/asc-devkit-8.5.0/examples/01_utilities/03_cpudebug

cmake -B build -DCMAKE_INSTALL_PREFIX=./ -DSOC_VERSION=Ascend910B1
cmake --build build -j

./build/add
# [SUCCESS][CORE_0] exit success!
# [SUCCESS][CORE_7] exit success!
# error ratio: 0.0000, test pass
```

### 一键运行

```bash
bash run_cpudebug_add.sh
```

---

## 6. 方法五：GDB 调试内核代码

### 6.1 CPU Debug 模式下的 GDB（推荐）

CPU Debug 模式下，kernel 代码被编译为普通 C++ 代码，可以直接用 GDB 调试：

```bash
cd /path/to/build

# 启动 GDB
gdb --args ./add

# GDB 内常用操作
(gdb) set follow-fork-mode child       # 跟踪子进程（多 core 时）
(gdb) break add_custom                 # 在 kernel 函数入口打断点
(gdb) break compute                    # 在计算函数打断点
(gdb) run
(gdb) next                             # 单步执行
(gdb) print variable_name              # 打印变量
(gdb) info locals                      # 查看所有局部变量
(gdb) continue                         # 继续执行
(gdb) bt                               # 查看调用栈
```

### 6.2 PEM 仿真模式下的 GDB

```bash
# 设置仿真库路径
export LD_LIBRARY_PATH=${SIMDIR}:${ASCENDLIB}:${DEVLIB}:${LD_LIBRARY_PATH}

# GDB 启动
gdb --args env LD_PRELOAD="libnpu_drv.so:libruntime_cmodel.so:libpem_davinci.so" ./add

(gdb) run
# PEM 模式下 GDB 主要调试 host 端代码（main 函数、内存分配等）
# 内核代码由 PEM 仿真器执行，不能直接断点到 kernel 内
```

---

## 7. 方法六：mssanitizer 正确性检查

`mssanitizer` 是 CANN 自带的算子正确性检查工具，支持内存泄漏、数据竞争等检测。

### 工具类型

| 工具 | 检查内容 |
|------|----------|
| `memcheck` | 内存泄漏、越界访问、未初始化内存使用 |
| `racecheck` | 数据竞争（多 core 并发访问冲突） |
| `initcheck` | 未初始化变量使用 |
| `synccheck` | 同步屏障正确性 |

### 用法

```bash
source /etc/profile.d/ascend.sh

# 进入算子的 build-cpu 目录
cd /path/to/build-cpu

# 设置仿真库路径
export LD_LIBRARY_PATH=${SIMDIR}:${ASCENDLIB}:${DEVLIB}:${LD_LIBRARY_PATH}

# 内存检查
mssanitizer --tool=memcheck --leak-check=yes \
    --kernel-name=add_custom \
    env LD_PRELOAD="libnpu_drv.so:libruntime_cmodel.so:libpem_davinci.so" \
    ./add

# 数据竞争检查
mssanitizer --tool=racecheck \
    --kernel-name=add_custom \
    env LD_PRELOAD="libnpu_drv.so:libruntime_cmodel.so:libpem_davinci.so" \
    ./add

# 指定日志文件
mssanitizer --tool=memcheck \
    --log-file=./sanitizer_output.log \
    --kernel-name=add_custom \
    env LD_PRELOAD="libnpu_drv.so:libruntime_cmodel.so:libpem_davinci.so" \
    ./add
```

---

## 8. 方法七：msopgen sim 流水线可视化

`msopgen sim` 从仿真 dump 数据生成 `trace.json`，可在 Chrome 中查看各单元的流水线：**VECTOR, SCALAR, CUBE, MTE1, MTE2, MTE3, FLOWCTRL, ICmiss**。

### 用法

```bash
# 仿真运行后（产生 dump 数据）

msopgen sim \
    -c 0 \                          # 要分析的 Core ID
    -d ./OPPROF_xxx/device0/ \      # dump 数据目录
    -subc cubecore0 \               # SubCore ID（Ascend910B 需要）
    -out ./output/ \                # 输出目录
    -reloc ./add                    # 算子的 .o 或可执行文件

# 输出: output/trace.json
# 在 Chrome 打开 chrome://tracing，加载该文件
```

---

## 9. 方法对比速查

| 方法 | 用途 | 启动方式 | 周期精确 | GDB 可用 | 难度 |
|------|------|----------|----------|----------|------|
| **PEM 仿真** | 运行验证 + cycle profiling | `LD_PRELOAD=... ./demo` | ✅ | ⚠️ host端 | ⭐ |
| **PEM toml** | 各 core/block cycle 分析 | 同上，自动生成 | ✅ | - | ⭐ |
| **msprof op simulator** | 硬件指标采样 | `msprof op simulator ./demo` | ✅ | ❌ | ⭐⭐ |
| **CPU Debug** | printf 调试 + GDB | 直接 `./add` | ❌ | ✅ 全部 | ⭐ |
| **GDB 调试** | 逐行调试 kernel | `gdb --args ./add` | ❌ | ✅ 全部 | ⭐⭐ |
| **mssanitizer** | 内存/并发正确性 | `mssanitizer --tool=... ./demo` | ✅ | ❌ | ⭐⭐ |
| **msopgen sim** | 流水线可视化 | `msopgen sim -c 0 -d dump/ -out ./` | ✅ | - | ⭐⭐ |

### 推荐工作流

```
编写算子 (.asc)
      │
      ▼
编译 (cmake + ASC 编译器)
      │
      ├──→ [PEM 仿真] 快速验证正确性，看 cycle 数
      │         ├── 通过 → [PEM toml] 分析各 core 负载均衡
      │         └── 失败 → [CPU Debug + GDB] 逐行调试
      │
      ├──→ [mssanitizer] 检查内存泄漏/数据竞争
      │
      └──→ [msopgen sim] 生成 chrome://tracing 流水线图
                │
                └──→ 性能优化 → 重复

(最终) → 真机 NPU 上板验证
```
